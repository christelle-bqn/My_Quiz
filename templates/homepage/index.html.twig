{% extends "base.html.twig" %}

{% block title %}My Quizz{% endblock %}

{% block body %}
        <div id="container-homepage">
            <div class="row">
                <div class="col d-flex flex-column align-items-center">
                    <h1 class="title-homepage">My Quizz</h1>
                    <div class="container-links d-flex flex-column align-items-center">
                        <a class="button-redirection" href="{{ path('quiz_index') }}">Jouer en tant qu'invité</a>
                        <a class="button-redirection" href="{{ path('app_register') }}">Inscription</a>
                        <a class="button-redirection" href="{{ path('app_login') }}">Connexion</a>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
